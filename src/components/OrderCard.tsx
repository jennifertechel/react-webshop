import {
  Box,
  Card,
  CardBody,
  Flex,
  Image,
  Spacer,
  Text,
} from "@chakra-ui/react";
import { CartItem } from "../../data";

function OrderCard({ product }: { product: CartItem }) {
  return <Card></Card>;
}

export default OrderCard;
